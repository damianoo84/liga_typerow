<!DOCTYPE html>
<html class="no-js">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title>Liga Typerów</title>
        <link rel="icon" type="image/png" href="/favicon.png">
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
        {% endblock %}
        
        <script src="{{ asset('https://ajax.googleapis.com/ajax/libs/jquery/1.12.2/jquery.min.js') }}"></script>
    </head>
    <body>
        
        <div id="header_01">
            <img src="{{ asset('images/mainphoto.jpg') }}" />
        </div>
        
        <div id="info_02">
            {% block info %}
                {% if app.user %}
                    {% for user in log_users() %}
                        {{ user.username }}
                    {% endfor %}
                {% endif %}
                {% if curr_matchday() is empty %}
                    Kolejny sezon już wkrótce
                {% else %}
                    {{ curr_matchday().name }}
                    {{ curr_matchday().dateFrom|date('d-m-Y') }} 
                    {{ curr_matchday().dateTo|date('d-m-Y') }}
                {% endif %}
            {% endblock %}
        </div>
        
        <div id="menu_03">
            <ul class="navigation" id="myNav">
                <li><a href="{{ path('liga_typerow_table') }}">TABELA</a></li>
                <li><a href="{{ path('liga_typerow_types') }}">TYPY</a></li>
                <li><a href="{{ path('liga_typerow_statistics') }}">STATYSTYKI</a></li>
                <li><a href="{{ path('liga_typerow_ranking') }}">RANKING</a></li>
                <li><a href="{{ path('liga_typerow_records') }}">REKORDY</a></li>
                <li><a href="{{ path('liga_typerow_history') }}">HISTORIA</a></li>
                <li><a href="{{ path('liga_typerow_principles') }}">ZASADY</a></li>
                <li><a href="{{ path('liga_typerow_forum') }}">FORUM</a></li>
                <li><a href="{{ path('liga_typerow_account') }}">KONTO</a></li>
                {% if app.user %}
                <li><a href="{{ path('_logout') }}">WYLOGUJ</a></li>
                {% else %}
                <li><a href="{{ path('liga_typerow_login') }}">ZALOGUJ</a></li>
                {% endif %}
                <li class="icon">
                    <a href="javascript:void(0);" >&#9776;</a>
                </li>
            </ul>
        </div>
        
        <div id="content_04">
            {% block content %}
            {% endblock %}
        </div>
        
        <div id="footer_05">
            <img src="{{ asset('images/footer.jpg') }}" />
        </div>
        
        <script type="text/javascript">
            $(document).ready(function(){
                $('li.icon').click(function() {
                    $('#myNav').toggleClass('responsive');
                });
            });
        </script>
        {% block javascripts %}
            <script src="{{ asset('js/jquery-2.2.4.min.js') }}"></script>
        {% endblock %}
    </body>
</html>